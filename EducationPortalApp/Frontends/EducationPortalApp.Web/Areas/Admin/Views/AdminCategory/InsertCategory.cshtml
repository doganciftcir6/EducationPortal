@using System.Text.Json;
@model CategoryCreateInput

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h5 class="card-title mb-0">Create New Category</h5>
                </div>
                <div class="card-body">
                    <form asp-controller="AdminCategory" asp-action="InsertCategory" method="post">
                        <div class="form-group">
                            <label for="categoryName">Category Name</label>
                            <input asp-for="@Model.CategoryName" id="categoryName" class="form-control" />
                            <span asp-validation-for="@Model.CategoryName" class="text-danger"></span>
                        </div>

                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Create</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
